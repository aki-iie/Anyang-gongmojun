# WORKPLAN — 원안 실현 및 허위 제거

> 작성 2026-09-17 · 마감 2026-09-21 (D-4)
> 적대적 감사 결과 반영. **A는 오늘 안에, B는 내일 안에.**
> 이 세션에서 역할 구분 없이 전부 처리한다.

---

## 실현할 원안

```
1. 반지하 거주민이 휴대폰으로 침수 가능성을 진단한다 (채팅 위주, 사진 보조)
2. 진단 결과가 데이터베이스에 남는다
3. 공무원이 데이터베이스를 확인한다
4. 침수 위험이 높은 가구에 공무원이 선조치를 취한다
```

**1~3은 코드로 닫는다. 4는 안양시의 결정이므로 화면에서 약속하지 않는다.**
우리가 만드는 것은 "공무원이 볼 수 있는 구조"까지이고, 도입 여부는 제안 영역이다.

---

# A. 오늘 안에 — 사실이 아닌 것 제거

코드에 사실이 아닌 문장이 있으면 그건 버그보다 나쁘다.

## A-1. 지원 대상 단정 제거

**`src/content.ts:61-62, 66`**

현재: 50점 이상이면 `"안양시 설치 지원 대상에 해당합니다"`
문제: 50점은 우리가 정한 임계값이다. 안양시 지원사업에는 점수 기준이 존재하지 않는다.

```
변경 전  안양시 설치 지원 대상에 해당합니다
변경 후  차수 설비 설치를 검토해 볼 수준입니다
```

`src/App.tsx:453, :949`의 `"가운데 선(50점)이 넘으면 안양시 설치 지원 대상"` 도 같이 고친다.

```
변경 후  가운데 선을 넘으면 차수 설비 설치를 우선 검토할 수준입니다
```

**`SUPPORT_THRESHOLD` 라는 이름 자체가 오해를 부른다.** `REVIEW_THRESHOLD` 로 바꾸고,
"지원(support)" 이라는 단어를 임계값 관련 식별자·주석·화면에서 전부 제거한다.

## A-2. 접수 약속 제거

**`src/App.tsx:464, :1097, :1111`**

현재: 난수 접수번호 + `"담당 공무원이 순차적으로 연락드립니다"`
문제: 안양시와 협의한 바 없다. 연락처 입력 필드조차 없다. **거주자를 기다리게 만든다.**

B-1(Firestore) 구현 전까지는 접수 버튼과 접수번호 UI를 **비활성화**한다.
B-1 완료 후 아래 문구로 되살린다.

```
진단 결과가 저장되었습니다.

이 데이터는 행정 대시보드에 동 단위로 집계됩니다.
현재는 시범 운영 단계이며, 개별 연락은 이루어지지 않습니다.
```

동의 문구에서 **"안양시로 전달"** 표현을 뺀다. 아직 전달 경로가 없다.

## A-3. 조회 실패 시 점수 가산 제거

**`src/content.ts:57-59` · `src/App.tsx:391-394`**

현재: 침수지도 조회 실패·미조회에도 `"50년 빈도 약 0.3m (예시 데이터)"` 30점이 총점에 합산
문제: 근거 없는 점수가 진단에 들어간다. **'낮음' 등급이 구조적으로 도달 불가능하다.**

```
조회 성공  → 실제 점수 반영
조회 실패  → 0점 + 화면에 "침수지도 조회 실패 — 이 항목은 점수에 반영되지 않았습니다"
미조회     → 진단 시작 자체를 막는다 (사진 업로드 버튼에 가드)
```

조회 실패 시 총점 만점 표기도 낮춘다. 없는 항목을 분모에 넣지 않는다.

## A-4. 가짜 AI 결과 제거

**`src/sync/openai.ts:42, :68, :70, :293, :356`**

현재: API 실패 시 사진과 무관한 고정값(턱 20·창문 12·이력 5)이 **"AI로 분석했습니다"** 와 함께 표시.
사진 0장에도 `"3장을 정밀 분석하는 중"` 대기 문구 출력.

```
API 실패    → "사진 분석에 실패했습니다. 질문에 직접 답해 주세요." + 문진으로 전환
사진 0장    → 대기 문구·분석 ack 출력 금지
simulateAnalysis → 개발용으로만 남기고 프로덕션 경로에서 제거
```

`src/content.ts:8` 의 `WAIT` 고정 문구 중 `"카드 높이(약 8.5cm) 내외로 추정"` 처럼
**관찰 결과인 척하는 문장**을 전부 뺀다.

## A-5. 점수를 LLM이 생성하지 않게

**`src/sync/openai.ts:218-222, :247, :290` · `src/App.tsx:362, :370, :378`**

현재: Vision 프롬프트에 배점표를 넣어 LLM이 보기별 점수를 직접 생성.
App이 그것을 `content.ts` 고정값보다 우선 채택. temperature 0.2, seed 없음.
→ **같은 사진에 다른 점수가 나온다.** 총점 중 최대 65점이 LLM 생성값.

```
프롬프트에서 배점표·점수 관련 지시를 전부 제거한다.
LLM 출력 스키마에서 score 필드를 제거한다.
LLM 은 '어느 보기에 해당하는가' 만 고른다.
점수는 content.ts 고정값에서만 가져온다.
temperature 0 으로 낮춘다.
```

`normalizeOptions` 가 상한만 클램프하고 있는데, **App 이 LLM score 를 읽는 경로 자체를 없앤다.**

## A-6. 사실 정정

| 항목 | 현재 | 수정 |
|---|---|---|
| 현관 물막이판 안양시 지원 | 계획서 p.5 "지원" ↔ 코드 "미지원" | **미지원**으로 통일 (담당부서 통화 확인) |
| 창문 차수막 | 화면 "무료로 설치" ↔ 계획서 "자부담 30만 원" | 실제 확인된 값으로 통일. 불확실하면 금액 언급 삭제 |
| 침수흔적 위치 | `flood.js:18` "박달동 6건" ↔ README "석수동 5·박달동 1·비산동 2" | 데이터로 재확인 후 통일 |
| 만점 | 이론상 95, 화면 `/100` | 실제 만점으로 표기 통일 또는 캡 구조 변경 |

## A-7. 후속 대화 컨텍스트 정리

**`src/App.tsx:450-455`**

현재: 후속 대화 LLM 에 `"50점 이상이면 안양시 지원 대상"` 을 **사실로 주입**.
→ 챗봇이 자유 문장으로 행정 기준을 확장 서술한다.

```
resultSummary 에서 '지원 대상' 표현 제거
시스템 프롬프트에 추가: "행정 지원 대상 여부나 지원 금액을 단정하지 마라.
확인되지 않은 제도를 설명하지 마라."
temperature 0.7 → 0.3
```

---

# B. 내일 안에 — 원안 2·3단계 구현

## B-1. Firestore 연결

### 스키마 (Firestore)

```
diagnoses/{docId}
  createdAt        timestamp
  emdCode          string     행정동 코드
  gridId           string     100m 격자 ID (개인 식별 방지)
  ── 위치 위험 ──
  floodDepth30y    number|null
  floodDepth50y    number|null
  floodSeg         string|null   N330~N334
  traceHit         boolean
  ── 구조 (문진) ──
  entranceSill     string     없음|카드보다낮음|카드와비슷|카드보다높음|unknown
  windowBase       string     땅보다낮음|비슷|땅보다높음|unknown
  waterBarrier     string     설치|미설치|unknown
  floodHistory     string     있음|없음|모름
  ── 역류 (문진) ──
  backflowValve    string     있음|없음|unknown
  rainySymptom     string     있음|없음|unknown
  ── 판정 ──
  totalScore       number
  level            string
  backflowStatus   string     양호|미흡|매우미흡|확인필요
  ── 품질 ──
  unknownCount     number
  photoUsed        boolean
  ── 동의 ──
  consentAdmin     boolean
  contact          string|null   동의한 경우에만
```

**상세 주소와 사진 원본은 저장하지 않는다.** 좌표는 100m 격자로 스냅한다.
이 설계를 사업계획서에 명시한다.

### 보안 규칙

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /diagnoses/{doc} {
      allow create: if request.resource.data.keys().hasAll(['createdAt','emdCode','totalScore'])
                    && request.resource.data.totalScore is number;
      allow read, update, delete: if false;   // 읽기는 서버 함수로만
    }
  }
}
```

클라이언트는 **쓰기만** 가능하다. 대시보드 읽기는 Cloud Function 을 거친다.

### 엔드포인트

```
POST /save        진단 결과 저장 → { ok, docId }
GET  /stats       동별 집계 반환 (대시보드용, 개별 문서 노출 금지)
```

`/stats` 는 개별 가구를 반환하지 않는다. 행정동 단위 집계만 반환한다.

## B-2. 연락처 입력

동의 체크 + 연락처 입력 필드를 추가한다.
**동의하지 않으면 `contact` 를 저장하지 않는다.** 진단 결과는 동의 여부와 무관하게 저장(비식별).

화면 문구:

```
[ ] 진단 결과를 행정 데이터로 활용하는 데 동의합니다
    (동 단위로만 집계되며 상세 주소·사진은 저장되지 않습니다)

[ ] 지원사업 안내를 받고 싶습니다 (선택)
    연락처: [          ]
    * 현재는 시범 운영 단계로 개별 연락은 이루어지지 않습니다
```

## B-3. 대시보드가 실제 데이터를 읽게

**`public/assets/data/dashboard.html`**

현재: DS2 격자 CSV 2개만 fetch. 진단 결과를 한 건도 읽지 않는다.

```
추가할 섹션 (기존 DS2 섹션 위에 배치)
  · 누적 진단 가구 수
  · 동별 진단 건수
  · 등급 분포 (높음/주의/보통/낮음)
  · 역류 대비 미흡 비율
  · 차수 설비 미설치 비율
  · 최근 진단 시각
```

`/stats` 를 fetch 한다. **데이터가 0건이면 "아직 진단 결과가 없습니다"를 표시**하고
DS2 섹션은 그대로 보여준다. 빈 상태에서도 화면이 깨지지 않아야 한다.

## B-4. 앱에서 대시보드 연결

`firebase.json` rewrites 에 `/admin` → `/assets/data/dashboard.html` 추가.
결과 화면 하단에 링크 하나.

## B-5. 역류 문항 추가

`docs/INTEGRATION_역류카드.md` 참조. 문항 2개 + 결과 카드 1개.
**역류 문항은 총점에 넣지 않는다.** 별도 카드로만 표시한다.

## B-6. 타임아웃

`src/sync/openai.ts` 의 fetch 에 `AbortController` 8초.
실패 시 A-4 의 폴백 경로로 떨어진다.

---

# C. 검증

A·B 완료 후 아래를 전부 통과해야 한다.

```
[ ] 주소 미입력 상태에서 진단 시작 불가
[ ] 침수지도 조회 실패 시 그 항목 0점 + 화면에 실패 표시
[ ] 사진 0장으로 진단 완주 가능, 가짜 분석 문구 없음
[ ] 같은 입력 3회 반복 시 총점 동일
[ ] LLM 강제 실패(네트워크 차단) 시 8초 내 문진으로 전환
[ ] 진단 완료 → Firestore 에 문서 1건 생성
[ ] /admin 에서 그 건이 동별 집계에 반영
[ ] 진단 0건 상태에서도 /admin 이 깨지지 않음
[ ] 화면 어디에도 "안양시 지원 대상", "공무원이 연락" 문구 없음
[ ] npm run lint 0 errors
[ ] python scripts/test_diagnose.py 전체 통과
```

---

# D. 하지 말 것

- **총점 배점(30/30/20/15)과 등급 경계(70/50/30)를 이번에 손대지 마라.**
  근거가 없는 건 맞지만, D-4 에 판정 체계를 바꾸면 전부 다시 검증해야 한다.
  사업계획서에 "서열만 반영한 잠정값"으로 **정직하게 기재**하는 것으로 대응한다.
- 새 기능 추가 금지. 위 목록 밖의 것은 만들지 않는다.
- `scripts/`, `docs/`, 노트북은 이번 작업 범위 밖이다.

---

# E. 작업 순서

```
1. A-1 ~ A-7  (사실 정정 — 화면 문구와 점수 경로)
2. lint + 수동 확인
3. 커밋: "fix: 사실이 아닌 문구 제거 및 LLM 점수 생성 경로 차단"
4. B-1 ~ B-2  (Firestore + 저장)
5. 커밋: "feat: 진단 결과 Firestore 저장 및 동의 기반 연락처 수집"
6. B-3 ~ B-4  (대시보드 연동)
7. 커밋: "feat: 행정 대시보드 실데이터 연동 및 /admin 라우트"
8. B-5 ~ B-6  (역류 카드 + 타임아웃)
9. 커밋: "feat: 역류 대비 카드 및 LLM 타임아웃 폴백"
10. C 검증 전체 통과 확인
11. 배포
```

각 단계 완료 시 무엇을 바꿨는지 한 줄로 보고할 것.
