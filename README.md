# 잠길까 — 안양시 반지하 침수 위험 진단

2026 안양시 공공데이터·AI 활용 대학생 경진대회 출품작.
반지하 거주자가 **집 사진 3장과 위치**를 넣으면, AI가 구조적 취약 요인을 묻고 **안양시 도시침수지도**와 결합해 침수 위험도를 점수로 내는 웹 서비스.

**라이브 · https://jamgilkka.web.app**

| | |
|---|---|
| 접수 마감 | 2026-09-21 (월) |
| 팀 | 차동현(질문지·현장 실사) · 류서현(웹·챗봇·DB) · 김황현(대비책·정책 조사) |
| 시범 범위 | 안양시 만안구·동안구 (충훈동·석수동 중심) |

---

## 1. 사용자가 겪는 흐름

**모바일** (실제 사용자 기준)

```
1단계 · 우리 집 위치      주소 검색 또는 현재 위치 → 지도로 확인
2단계 · 사진 3장          현관 턱 → 창문 → 골목 (카메라 바로 실행)
3단계 · 문진              AI가 사진을 보고 애매한 부분만 되물음
4단계 · 결과              총점 + 침수지도 오버레이 + 대비책 + 후속 대화
```

**데스크톱**은 1·2단계가 한 화면에 함께 나온다.

### 지도는 두 번 나오고, 역할이 다르다

- **1단계** — 배경지도 + 핀만. *"여기가 우리 집이 맞나요?"* 를 확인하는 화면이라 침수 구역을 덮지 않는다.
- **4단계** — 같은 지도에 침수 레이어가 얕은 등급부터 차례로 차오르고, 핀 위에 `N334 · +30점` 배지가 새겨진다.

---

## 2. 점수는 이렇게 나온다

총 100점 만점. **침수지도(지역) + 구조 요인(가구)** 의 합이다.

| 항목 | 배점 | 출처 |
|---|---|---|
| **침수 위험 (두 채널 합산)** | 0~30 | 실제 좌표 판정 |
| 현관 턱 높이 | 0~30 | 사진 + 문진 |
| 창문 아래쪽 높이 | 0~20 | 사진 + 문진 |
| 골목 침수 이력 (2022.8 앵커) | 0~15 | 문진 |

**침수 위험 0~30점은 두 채널로 낸다.**

| 채널 | 성격 | 데이터 | 배점 |
|---|---|---|---|
| **A** 도시침수지도 | 예측 — 이만큼 비가 오면 잠길 것 | 30년·50년 빈도 폴리곤 | 0~30 |
| **B** 침수흔적도 | 실적 — 실제로 잠겼던 자리 | 2022년 등 침수 기록 | 0~30 |

합산 규칙은 `min(30, max(A, B) + 겹치면 5)`. 단순히 더하지 않는 이유는 총점이 `/100` 으로 표시되는데
현재 최대가 95점(30+30+20+15)이라 그냥 더하면 100을 넘기 때문이다. 근거(`basis`)는 화면에 그대로 나온다.

**50점 이상**이면 안양시 설치 지원 대상, **70점 이상**은 "매우 높음".

> **사진만으로 침수 정도는 판단할 수 없다.** 사진은 *"물이 들어오기 쉬운 구조인가"* 를 답하고, 침수지도는 *"이 자리에 물이 얼마나 차는가"* 를 답한다. 사진 한 장에는 주변 지형 고저차도, 하수관 용량도 담기지 않는다. 그래서 둘 다 필요하다.

---

## 3. 파일 구조 — 무엇을 어디서 고치나

```
안양 공모전 웹/
├── index.html                브라우저 탭 제목
├── .env                      ★ 클라이언트 키 (커밋 금지)
│
├── src/
│   ├── content.ts            ★★★ 질문·보기·배점·결과 문구 — 대부분의 수정은 여기
│   ├── App.tsx               화면 UI 전체 (1,080줄, 4개 화면 상태머신)
│   ├── FloodMap.tsx          Leaflet 지도 — 배경타일 + 침수 폴리곤 + 핀 + 배지
│   ├── RainCanvas.tsx        하단 빗방울 연출
│   ├── useIsMobile.ts        768px 이하 판별
│   ├── css.ts                'font-size:20px' 문자열 → React style 객체
│   ├── theme.css             색·폰트 토큰
│   ├── broadsheet.css        버튼·입력·다이얼로그 기본 스타일
│   └── sync/
│       ├── openai.ts         ★★ AI 프롬프트 3종 + 응답 정규화
│       ├── flood.ts          침수 조회 · GPS · ★ 위치 켜는 법 안내
│       └── address.ts        주소 검색(다음) + 좌표 변환 호출
│
├── functions/                서버 (Cloud Functions)
│   ├── index.js              엔드포인트 3개 정의
│   ├── flood.js              ★★ 침수 등급 이름·배점, 판정 로직
│   ├── geocode.js            주소 → 좌표 (VWorld)
│   ├── flood30.json          30년 빈도 폴리곤 1.2MB (건드리지 말 것)
│   ├── flood50.json          50년 빈도 폴리곤 1.3MB (건드리지 말 것)
│   └── .env                  ★ 서버 키 (커밋 금지)
│
└── public/                   ex-sill · ex-window · ex-alley (촬영 예시 사진)
```

★ 표시가 많을수록 자주 손대는 파일.

### 자주 하는 수정 — 어디를 열면 되나

| 하고 싶은 일 | 파일 | 위치 |
|---|---|---|
| 문진 **질문·보기·점수** 바꾸기 | `src/content.ts` | `QUESTIONS` |
| 등급 문구("침수 위험 높음") | `src/content.ts` | `LEVEL_DESC` |
| 등급 경계(70/50/30점) | `src/content.ts` | `levelOf()` |
| **대비책 추천** 문구·조건 | `src/content.ts` | `recommendationsOf()` |
| 지원 기준 점수(50점) | `src/content.ts` | `SUPPORT_THRESHOLD` |
| **담당 부서 전화번호** | `src/content.ts` | `CONTACT` ← 지금 예시 번호 |
| 분석 중 문구("하늘에게 물어보는 중…") | `src/content.ts` | `WAIT` |
| 화면 제목·버튼·안내문 | `src/App.tsx` | 문구로 `grep` |
| **AI가 사진에서 볼 것** | `src/sync/openai.ts` | Vision 프롬프트 |
| 위치 켜는 법 안내 | `src/sync/flood.ts` | `locationHelp()` |
| **침수 등급 이름·배점** | `functions/flood.js` | `LEVEL` |

> **팁** — 화면에 보이는 문구는 거의 다 따옴표 안에 그대로 있다. 찾을 때는:
> ```bash
> grep -rn "바꾸고 싶은 문구" src/
> ```

---

## 4. 개발과 배포

```bash
npm install          # 최초 1회
npm run dev          # 로컬 개발 서버 (저장하면 즉시 반영)
npm run lint         # 코드 점검 — 현재 오류 0 · 경고 0
npm run build        # dist/ 생성 (tsc + vite)
```

### 배포

```bash
# src/ 를 고쳤으면
npm run build && firebase deploy --only hosting

# functions/ 를 고쳤으면
firebase deploy --only functions
```

**로컬(`npm run dev`)에서는 `/api/*` 가 없다.** 침수 판정·주소 검색·AI는 배포본에서만 동작한다. 화면 문구나 레이아웃만 볼 때는 로컬이 빠르다.

**배포 후 화면이 안 바뀌면** `index.html` 캐시다. 주소 뒤에 `?v=2` 를 붙이거나 강력 새로고침하면 된다.

---

## 5. 기술 구성

### 스택
Vite 8 · React 19 · TypeScript · Leaflet · Firebase(Hosting + Cloud Functions Node 22)
CSS 프레임워크 없이 인라인 스타일 헬퍼 `css()` 사용. 서체 Noto Serif KR.

### AI
**gpt-4o-mini** (OpenRouter 경유). 호출 3종:

| 용도 | 설정 |
|---|---|
| 사진 분석 (Vision) | temp 0.2, JSON 모드 |
| 자유 텍스트 → 보기 매핑 | temp 0, JSON 모드 (로컬 매칭 실패 시에만) |
| 자유 대화 | temp 0.7 |

**실측 비용 — 진단 1회 약 $0.0066 (9원).** prompt 토큰 42,000개 중 대부분이 사진이다. 업로드 시 긴 변 1024px로 줄이면 비용·속도가 크게 개선된다(미적용).

AI 응답은 `normalizeVLM()` 을 반드시 통과한다. 보기가 비었거나 형태가 어긋나면 `content.ts` 기본값으로 폴백하므로 **문진이 멈추지 않는다.**

### 문진이 막히지 않는 세 경로
버튼 클릭 · 자유 텍스트 · 건너뛰기 모두 `commitAnswer()` 하나로 모인다.
자유 텍스트는 로컬 매칭 → 실패 시 AI 매핑. 그래도 안 되면 선택지를 다시 띄운다.
되묻는 질문("차수판이 뭐예요?")은 답한 뒤 원래 질문으로 돌아온다.

### 키 보안
- **클라이언트**: `getApiKey()` 가 `import.meta.env.DEV` 로 게이트되어, 배포 빌드에서는 `() => null` 로 상수 폴딩된다. **번들에 AI 키가 들어가지 않는다** (매 배포마다 확인).
- **배포본**은 `/api/llm` 프록시를 탄다. 서버 키는 `functions/.env`.
- **프록시 가드레일**: 모델 화이트리스트(gpt-4o-mini만) · origin 검사 · 메시지 40개 상한 · max_tokens 1200. 외부 origin 403, 허용 외 모델 400으로 검증됨.
- VWorld 키는 지도 타일용이라 클라이언트에 노출된다(지도 API 표준 관행).

---

## 6. 침수지도 연동

원본: 국가공간정보 **도시침수지도 30년·50년 빈도** SHP — 만안구(41171)·동안구(41173).
좌표계 **EPSG:5186**(KGD2002 중부원점) → WGS84 변환 후 `functions/flood*.json` 에 적재.

- 속성 `SEG_CODE` **N330~N334** 가 침수심 등급. **숫자가 클수록 깊다** — 면적(N330 8.35km² → N334 0.001km²)과 폴리곤 포함관계로 확인.
- **각 등급의 실제 m 구간은 아직 확정되지 않았다.** 현재 배점(30/28/25/20/12)은 순서만 반영한 잠정값. 원본 배포처 범례를 확인하면 `functions/flood.js` 의 `LEVEL` 표 한 곳만 고치면 화면·점수·지도가 모두 따라온다.
- 판정은 **서버에서만** 한다. 지도 데이터 2.5MB는 브라우저로 내려가지 않는다. 판정 200회 7ms.
- 30년·50년을 각각 판정해 **보수적인 쪽(50년)** 을 점수로 쓰고, 두 빈도를 화면에 함께 보여준다.

**검증** — 충훈동(석수동 충훈부) 좌표가 "침수 예상 구역 아님"으로 나온다. 기획서의 *"충훈동은 침수흔적도상 이력은 있으나 도시침수지도상 위험 지역이 아니다"* 와 일치하며, 아래 채널 B가 그 "이력"을 실제로 찾아낸다.

**침수 면적 비교** (단위 km², 만안+동안 합계)

| 등급 | 30년 | 50년 | 증가 |
|---|---|---|---|
| N330 | 8.353 | 8.608 | +3.1% |
| N331 | 0.465 | 0.517 | +11.2% |
| N332 | 0.085 | 0.097 | +13.5% |

깊은 등급일수록 증가폭이 크다 — 비가 더 오면 넓어지기보다 **깊어진다.**

### 채널 B — 침수흔적도 (실제로 잠겼던 기록)

출처: **재난안전데이터공유플랫폼 `DSSP-IF-00117`** (JSON, 폴리곤 `GEOM` 포함).
전국 38,003건 중 **안양시 8건** (석수동 5 · 박달동 1 · 비산동 2). 좌표계 **EPSG:3857** → WGS84 변환 후 `functions/trace.json` (6.4KB).

```bash
SAFETYDATA_KEY=... python3 tools/build_trace.py     # 원본 재수집 → trace.json 재생성
```

- API에 **시군구 필터 파라미터가 없다.** 전량(1000건×39페이지)을 받아 `STDG_SGG_CD` 로 자른다. 원본 캐시(46MB)는 `.cache_trace` 에 남고 git 에는 올리지 않는다.
- 기록된 침수심(m)으로 배점한다 — `≥0.5m:30 / ≥0.3m:25 / ≥0.2m:20 / ≥0.1m:15 / 그 외:12`. **반지하는 0.2m만 차도 실내로 들어온다.**
- 지도에는 **붉은 점선**으로 그린다. 예측(파란 면)과 겹쳐 보면 두 채널의 차이가 바로 읽힌다.

**왜 두 채널이 필요한가 — 충훈동이 그 증거다**

안양시 8건의 법정동을 확정하고 채널 A로 교차 조회한 결과다.

| 법정동 | 건수 | 기록 침수심 | 채널 A(예측) | 최종 |
|---|---|---|---|---|
| **석수동** (충훈부 일대) | **5** | 0.197 ~ 0.33m | **모두 범위 밖 (0점)** | **20~25점** |
| 박달동 | 1 | 0.093m | 범위 밖 (0점) | 12점 |
| 비산동 (동안구) | 2 | 0.128 · 0.129m | N330 (12점) | 20점 |

석수동 5건은 **충훈로101번길 · 와룡로3번길 · 석수로202번길 · 석수로212번길** — 모두 **충훈부** 일대다.
실제 주소로 조회하면 이렇게 나온다.

| 주소 | 채널 A | 채널 B | 최종 |
|---|---|---|---|
| 충훈로101번길 55 | 0점 | 25점 (0.33m) | **25점** |
| 와룡로3번길 34-7 | 0점 | 20점 (0.262m) | **20점** |
| 석수로202번길 7-12 | 0점 | 20점 (0.249m) | **20점** |
| 석수로212번길 26 | 0점 | 20점 (0.249m) | **20점** |

**충훈동은 2022년 8월 호우로 실제 0.33m까지 잠겼는데, 도시침수지도는 "위험 없음"이라고 답한다.**
기획서의 *"충훈동은 침수흔적도상 이력은 있으나 도시침수지도상 위험 지역이 아니다"* 가 공공데이터로 그대로 확인된다.
예측지도 한 채널만 쓰면 충훈동 전체가 0점으로 나온다. 채널 B가 이를 20~25점으로 되살린다.

> **동 이름 주의** — 행정구역상 **충훈동이라는 법정동은 없다.** 충훈부는 **만안구 석수동**의 지역명이고,
> 충훈로·충훈고등학교가 그 일대에 있다. VWorld 지오코더에 "충훈동"을 넣으면 석수동 주소를 돌려준다.
>
> **OSM(Nominatim) 역지오코딩은 이 지역의 법정동을 박달동으로 잘못 준다.** 도로명(석수로212번길, 충훈로101번길)은
> 맞게 주지만 동 이름이 틀린다. 법정동 확정은 반드시 VWorld(국가 공식)로 해야 한다 — 오차 5~9m로 일치한다.
>
> 기록은 **2022년 한 해뿐**이라 표본이 얇다. 45가구 현장 실사가 이를 보강한다.

---

## 7. 위치 입력 세 가지

| 방법 | 키 | 비고 |
|---|---|---|
| **주소 검색** | 불필요 | 다음(카카오) 우편번호 → VWorld 지오코더로 좌표 변환 |
| **현재 위치** | 불필요 | 브라우저 Geolocation. 집에서 접속하면 가장 정확 |
| **URL 좌표** | 불필요 | `?lat=&lon=&acc=` — 현장에 가지 않고 시연·테스트 |

위치가 거부되면 **기기별(iPhone/Android/PC) 위치 켜는 법**이 화면에 단계별로 뜬다. 코드로 권한 팝업을 강제할 수는 없다(브라우저 보안 정책).

### 시연용 링크

```
가장 깊은 구간 (N334 · +30점)
https://jamgilkka.web.app/?lat=37.39908&lon=126.93219

30년 안전 → 50년 침수 (빈도 차이가 드러나는 자리)
https://jamgilkka.web.app/?lat=37.38428&lon=126.90114

충훈동 — 구역 밖 (기획서 검증 지점)
https://jamgilkka.web.app/?lat=37.3985&lon=126.9130
```

---

## 8. 서버 엔드포인트

| 경로 | 리전 | 하는 일 |
|---|---|---|
| `POST /api/llm` | us-central1 | AI 프록시. 키를 서버에서 붙여 대신 호출 |
| `GET /api/flood?lat=&lon=` | us-central1 | 두 채널 침수 판정. `&shape=1` 이면 예측·흔적 폴리곤도 반환 |
| `GET /api/geocode?address=` | **asia-northeast3** | 주소 → 좌표 |

> `geocode` 만 서울 리전인 이유 — **VWorld 지오코더가 해외 IP 연결을 끊는다**(`UND_ERR_SOCKET`). 국내 리전에서만 응답한다.

---

## 9. 아직 안 된 것

| 항목 | 상태 |
|---|---|
| 등급별 실제 침수심(m) 구간 | **원본 범례 확인 필요 — 가장 급함** |
| ~~침수흔적도 연동~~ | ✅ **완료** — 안양시 8건, 채널 B로 가동 중 |
| 루프(지붕 범위) 요인 | 기획서엔 있으나 미구현. 4요인으로 확장 검토 |
| Firestore 저장 | 스키마·rules 설계 완료, 팀 승인 대기 |
| 공무원 우선순위 화면 | 미착수 |
| 현장 라벨 기반 가중치 | 실사 데이터 대기 |
| 사진 리사이즈 | 미적용. 적용 시 비용·속도 개선 |

### 기획서와 달라진 점

- **DB**: Supabase → **Firebase(Firestore)**
- **모델**: Gemini/Qwen 예정 → **gpt-4o-mini** 확정
- **요인**: 턱·창문·루프 → 턱·창문·**골목이력** (루프 빠짐)
- **침수지도**: 배경 이미지 → **실제 좌표 판정으로 점수에 반영**

---

## 10. 저장소 규칙

- `.env`, `functions/.env` 는 **절대 커밋하지 않는다** (`.gitignore` 등록됨)
- `functions/flood*.json` 은 변환 결과물이다. 원본 SHP에서 다시 만들 수 있지만 직접 편집하지 말 것
- 커밋 전 `npm run lint` 로 확인 — 현재 **오류 0 · 경고 0**
